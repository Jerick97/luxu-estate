NEXT_PUBLIC_SUPABASE_URL=your_supabase_url_here
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Canonical production URL (no trailing slash), e.g. https://luxu-estate.vercel.app
# Used for SEO: metadataBase, Open Graph / Twitter card URLs and JSON-LD.
# On Vercel it falls back to VERCEL_URL automatically, but set this for a stable canonical domain.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
